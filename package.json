{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "packageManager": "yarn@3.2.1",
  "scripts": {
    "build": "yarn workspace rich-log build",
    "minify": "yarn workspace rich-log minify-js && yarn workspace rich-log minify-module",
    "dev": "yarn workspace rich-log dev",
    "test": "yarn workspace rich-log test",
    "test:ci": "yarn workspace rich-log test:ci",
    "lint": "yarn workspace rich-log lint",
    "example-react": "yarn workspace rich-log-react-example start",
    "example-preact": "yarn workspace rich-log-preact-example start",
    "postinstall": "husky install",
    "commit": "yarn git-cz",
    "release": "yarn workspace rich-log semantic-release"
  },
  "devDependencies": {
    "cz-conventional-changelog": "^3.3.0",
    "git-cz": "^4.9.0",
    "husky": "^8.0.1",
    "lint-staged": "^13.0.2",
    "prettier": "^2.7.1"
  },
  "resolutions": {
    "minimist": "^1.2.6"
  },
  "prettier": {
    "singleQuote": true,
    "trailingComma": "all",
    "printWidth": 120
  },
  "lint-staged": {
    "*.{ts,tsx,json}": [
      "prettier --write"
    ]
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  }
}
